import React from "react";

export default function AdminModeBar() {
  return (
    <div className="admin-bar">
      <div className="admin-bar-in">⚙ 관리자 모드 — 각 섹션에서 추가, 수정, 삭제할 수 있습니다.</div>
    </div>
  );
}
